input {  
	beats {
		port => "5044"
	}
}

filter {
	json {
    	source => "message"
	}
}

output {
	elasticsearch {
		user => "elastic"
    	password => "DBSxcN6U"
		hosts => "elasticsearch:9200"
	}
}